{% extends 'layout.html.twig' %}

{% block title %}
	{{parent()}}
	- Add Player
{% endblock %}

{% block content %}

	{# <p>
		{{player.firstname}}
		{{player.lastname}}
	<p> #}

	<p>
		Le joueur {{player.fullname}} a été ajouté.
	</p>

	<h2>
   {# Traversing #}
		{% for ticket in player.tickets %}
			<p>{{ticket.amount}}</p>
			<p>{{ticket.magicNumber}}</p>
			<p>{{ticket.player.birthdate | date('d-m-Y H-i-s')}}</p>
		{% endfor %}
	</h2>

{% endblock %}
